# BountyHound Local - Environment Variables
# Copy to .env and adjust as needed

# ── Redis ──────────────────────────────────────────────────────────
BHL_REDIS_URL=redis://localhost:6379/0
BHL_REDIS_RESULT=redis://localhost:6379/1

# ── Database ───────────────────────────────────────────────────────
# Bare metal:
BHL_DB_PATH=~/bountyhound-local/data/bountyhound.db
# Vast.ai:
# BHL_DB_PATH=/workspace/data/bountyhound.db

# ── Model Config ───────────────────────────────────────────────────
# Full precision (94GB H100 NVL):
BHL_CONFIG_PATH=./config/models.yaml
# AWQ quantized (80GB H100 SXM/PCIe or to save VRAM):
# BHL_CONFIG_PATH=./config/models-h100-awq.yaml

# ── HuggingFace (for model downloads) ─────────────────────────────
# HF_TOKEN=hf_xxxxx
# Bare metal:
# HF_HOME=~/.cache/huggingface
# Vast.ai:
# HF_HOME=/workspace/models

# ── Vast.ai ───────────────────────────────────────────────────────
# Set to 1 to enable Vast.ai mode (auto-detected if /workspace exists)
# BHL_VAST_AI=1
# PYTHONPATH=/workspace/bountyhound-local

# ── Advanced ──────────────────────────────────────────────────────
# Redis data directory (Vast.ai daemon mode)
# REDIS_DATA_DIR=/workspace/redis-data
# Bounty findings output directory
# BHL_FINDINGS_DIR=/workspace/bounty-findings
